# gbl

Git Branch List.

## Development

```bash
bun install

# Dev mode (from any git repository):
./gbl

# Build standalone binary:
bun run build

# Run the binary (no bun/node_modules needed):
./dist/gbl
```

Press `?` to see all keybindings.
